export enum MAIN_PATH {
    DEV = "dev",
    DIST = "dist",
}

export enum FOLDER {
    JS = "js",
    SCSS = "scss",
    CSS = "css",
    PARTIALS = "partials",
}

export enum EXT {
    TS = "ts",
    SCSS = "scss",
}
